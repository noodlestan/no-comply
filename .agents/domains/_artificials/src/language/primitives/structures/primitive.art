# Module

## Uses

::FROM `language/grammar/index.art` - Identifier, NaturalName, NaturalExpression, ExampleBlock

::FROM `language/primitives/index.art` - List, Record

::FROM `./_types.art` - PrimitiveName, PrimitiveStatus, PrimitivePurpose, PrimitiveDescription, PrimitiveSchema, PrimitiveParams, PrimitiveParam, PrimitiveArt, PrimitiveDeclaration, PrimitiveGeneration, PrimitiveConsumption, PrimitiveDiscovery

## Declares

### Primitive: Overload

#### Art

#### Declaration

#### Generation

#### Consumption

#### Discovery

### Structure: Primitive

**Purpose:** Represents the meta-model used to describe interpreter-defined primitive value types.

**Primitive:** Record

**Shape:**

- `name` - PrimitiveName
- `status` - PrimitiveStatus
- `purpose` - PrimitivePurpose
- `schema` - PrimitiveSchema
- `params` - PrimitiveParams
- `art` - Overload (PrimitiveArt)
- `declaration` - Overload (PrimitiveDeclaration)
- `generation` - Overload (PrimitiveGeneration)
- `consumption` - Overload (PrimitiveConsumption)
- `discovery` - Overload (PrimitiveDiscovery)

#### Art

#### Declaration

#### Generation

#### Consumption

#### Discovery
