// import { Select } from '@noodlestan/ui-system';
// import { themeStore } from '@noodlestan/ui-system';
import { Component } from 'solid-js';

import './ThemeSelect.css';

export const ThemeSelect: Component = () => {
    // const { theme, setTheme } = themeStore;

    // const handleThemeChange = (value: string) => setTheme(value);

    return (
        // <Select size="s" onChangeValue={handleThemeChange} length="auto" value={theme().name}>
        //     {/* <For each={THEMES}>{theme => <option value={theme.name}>{theme.name}</option>}</For> */}
        // </Select>
        <>TODO</>
    );
};
