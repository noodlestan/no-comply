import { createDemoSectionData } from '../../../../../../content';

import variant from './10-variant';
import tag from './40-tag';
import nowrap from './50-nowrap';
import aligned from './60-aligned';

export default createDemoSectionData({
    title: 'Props',
    items: [variant, tag, nowrap, aligned],
});
