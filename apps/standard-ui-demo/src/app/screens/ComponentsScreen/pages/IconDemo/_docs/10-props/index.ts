import { createDemoSection } from '../../../../../../components';

import size from './10-size';
import recoloring from './20-recoloring';

export default createDemoSection({
    title: 'Props',
    items: [size, recoloring],
});
