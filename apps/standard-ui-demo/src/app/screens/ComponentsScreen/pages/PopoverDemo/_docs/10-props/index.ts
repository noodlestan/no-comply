import { createDemoSectionData } from '../../../../../../content';

import onShow from './51-on-show';
import onHide from './52-on-hide';

export default createDemoSectionData({
    title: 'Props',
    items: [onShow, onHide],
});
