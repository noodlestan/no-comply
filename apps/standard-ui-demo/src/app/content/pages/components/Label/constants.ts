import type { DocsItemProps } from '../../../types';

export const itemProps: Partial<DocsItemProps> = {
    padding: 'l',
    styled: true,
};

export const itemPropsWrap: Partial<DocsItemProps> = {
    padding: 'l',
    styled: true,
    width: '280px',
};
