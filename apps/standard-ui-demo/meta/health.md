# Health — Standard UI Demo

> Generated by `manage-meta-health-file` skill (sub-agent).
> Date: 2026-06-25
> Package: `@no-comply/standard-ui-demo` v0.0.11

---

## Security

**#deps** Dev dependencies (`vite`, `vite-plugin-solid`, etc.) not pinned to exact versions — inherited from monorepo root

## Performance

**#bundle** In-browser TSX compiler bundles large TypeScript (`typescript` 5.9.3) and Babel (`@purrpose/client-babel`) dependencies as vendor chunks — increases initial load time

**#tree-shaking** Heavy monorepo dependency graph may prevent full tree-shaking of unused `@no-comply/*` exports

## Accessibility

## Architecture

**#coupling** Tight coupling to `@no-comply/*` monorepo ecosystem — package cannot be easily reused or extracted outside the monorepo

**#static** Static meta JSON bundled at build time — requires full rebuild for content changes; no hot-reload path for documentation content

**#dynamic** No runtime data fetching — simple and offline-capable, but limits dynamic/real-time use cases

## Conventions

**#consistency** `src/app/components/code/` lacks a `private/` directory despite containing complex internals (CodeBlock, JSXRenderer) — inconsistent with the rest of the codebase

**#barrel** Comprehensive barrel exports via `// @index` directives — risk of circular index imports if not carefully maintained

## Testing

**#tests** No test files found anywhere in the project — no unit, integration, or e2e tests exist

**#scripts** No `test` script defined in `package.json`

## DevX

**#rebuild** Any change to documentation content or meta JSON requires a full `vite build` — no incremental content update path

**#config** Linting and CI configurations inherited from monorepo root — no local `.eslintrc` or `.github/` workflows; local customization requires monorepo-level changes

## Documentation

## Dependencies

**#coupling** All 14 runtime dependencies are from the `@no-comply`, `@purrception`, `@purrtrait`, and `@purrpose` monorepo scopes — zero external library diversity

**#peer** No peer dependencies declared; relies on hoisting from monorepo root (`solid-js`, `typescript`, `@solidjs/router`)

## Issues

## CI/CD

**#scripts** No test or typecheck-only script; `ci` script runs `tsc --noEmit && vite build` but skips linting
