# Health — @no-comply/meta

> Generated by manage-meta-health-file skill (sub-agent).
> Date: 2026-06-25
> Location: `libs/meta/`

## Security

## Performance

## Accessibility

## Architecture

**#package** `package.json#files` lists `"scss"` but no `scss/` directory exists in the project root

## Conventions

## Testing

**#coverage** No test files found in the source tree — no `.spec.ts` or `.test.ts` files exist

## DevX

**#testing** No test suite means changes must be verified manually

## Documentation

## Dependencies

**#peer** `solid-js` is declared as a peer dependency but is not directly imported in source — purpose unclear

## Issues

**#unused-ref** `scss/` referenced in `files` field of `package.json` but directory is absent

## CI/CD
