# Modules — @no-comply/meta

> Generated by manage-meta-modules-file skill (sub-agent).
> Date: 2026-06-25
> Location: `libs/meta/`

Source tree of non-index, non-private files with one-line scope descriptions.

```
src/
├── types.ts                                          — NoComplyMetaAPI interface definition
├── createNoComplyMetaService.ts                      — Factory function, creates API from entity array
│
├── entities/
│   ├── types.ts                                      — Base NoComplyEntityPartial, NoComplyEntityData types
│   ├── helpers/
│   │   ├── getEntityFunctions.ts                     — Extracts FunctionDeclaration[] from entity symbols
│   │   └── getEntityTypes.ts                         — Extracts TypeDeclaration[] from entity symbols
│   │
│   ├── Component/
│   │   ├── types.ts                                  — ComponentEntityPartial, ComponentEntityData, ComponentEntityFiles
│   │   └── helpers/
│   │       ├── isNoComplyComponent.ts                — Type guard: dec.type === 'component'
│   │       ├── resolveComponentDeclaration.ts        — Resolves ComponentDeclaration by name
│   │       ├── resolveComponentFactoryDeclaration.ts — Resolves factory FunctionDeclaration
│   │       ├── resolveComponentProps.ts              — Extracts ObjectLiteralTypeNode from props
│   │       └── resolveComponentPropsJsDocData.ts     — Resolves JSDoc data for props type
│   │
│   ├── Context/
│   │   ├── types.ts                                  — ContextEntityPartial, ContextEntityData
│   │   └── helpers/
│   │       └── isNoComplyContext.ts                  — Type guard: dec.type === 'context'
│   │
│   ├── Controller/
│   │   ├── types.ts                                  — ControllerEntityPartial, ControllerEntityData
│   │   └── helpers/
│   │       └── isNoComplyController.ts               — Type guard: dec.type === 'controller'
│   │
│   ├── Mixin/
│   │   ├── types.ts                                  — MixinEntityPartial, MixinEntityData
│   │   └── helpers/
│   │       └── isNoComplyMixin.ts                    — Type guard: dec.type === 'mixin'
│   │
│   ├── Module/
│   │   ├── types.ts                                  — ModuleEntityPartial, ModuleEntityData
│   │   └── helpers/
│   │       └── isNoComplyModule.ts                   — Type guard: dec.type === 'module'
│   │
│   ├── Provider/
│   │   ├── types.ts                                  — ProviderEntityPartial, ProviderEntityData
│   │   └── helpers/
│   │       └── isNoComplyProvider.ts                 — Type guard: dec.type === 'provider'
│   │
│   └── Service/
│       ├── types.ts                                  — ServiceEntityPartial, ServiceEntityData
│       └── helpers/
│           └── isNoComplyService.ts                  — Type guard: dec.type === 'service'
│
└── private/                                          — (excluded from this map per convention)
```

## Excluded

- All `index.ts` barrel files (re-exports only — 17 files)
- All files under `src/private/` (internal implementation — 2 files)
