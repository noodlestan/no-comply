import { definePropKeys } from '@noodlestan/context-ui-primitives';

import type { PopoverContextOptions } from './types';

export const POPOVER_CONTEXT_OPTIONS = definePropKeys<PopoverContextOptions>()(['id']);
