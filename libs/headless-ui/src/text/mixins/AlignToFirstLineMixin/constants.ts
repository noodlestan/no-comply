import { definePropKeys } from '@noodlestan/context-ui-primitives';

import type { AlignToFirstLineMixinProps } from './types';

export const ALIGN_TO_FIRST_LINE_MIXIN_PROPS = definePropKeys<AlignToFirstLineMixinProps>()([
    'aligned',
]);
