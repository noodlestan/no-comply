import type { ClassList } from '@noodlestan/context-ui-primitives';

export type IconButtonMixinAPI = {
    $root: {
        classList: ClassList;
    };
    $icon: {
        classList: ClassList;
    };
};
