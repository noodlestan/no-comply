# Health — @purrception/lang-ts

> Generated by manage-meta-health-file skill
> Date: 2026-06-25
> by: sub-agent
> Package: `libs/purrception-lang-ts`

## Architecture

**#dead-code** `vite.config.mts` includes `vite-plugin-solid`, `vite-plugin-solid-svg`, `vite-plugin-top-level-await` — none relevant to this pure-type library; inherited from monorepo template

**#dead-config** Root `tsconfig.json` sets `"jsxImportSource": "solid-js"` — irrelevant for this package, no JSX/component code exists

**#redundancy** `build:clean` script deletes `dist/` which is already excluded in root `tsconfig.json` and `.prettierignore`

## Conventions

**#bug** `isTypeExpressionNode` implementation uses `!exp && typeof exp === 'object'` — negation before AND, always returns `false`. Should be `!!exp && typeof exp === 'object'`

## Testing

**#missing-tests** No test or spec files found despite vitest being configured and a `turbo-test.log` existing

## Dependencies

**#unused-plugins** Four vite plugins included in config but none are exercised by this package's build

## Issues

**#leaky-abstraction** `FunctionLikeDeclaration` references real TypeScript compiler API (`ts.FunctionDeclaration | ts.VariableStatement`) — breaks the otherwise abstracted AST model
