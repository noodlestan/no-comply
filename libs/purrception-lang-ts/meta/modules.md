# Modules — @purrception/lang-ts

> Generated by manage-meta-modules-file skill
> Date: 2026-06-25
> by: sub-agent
> Package: `libs/purrception-lang-ts`

```
src/
├── constants.ts                        — Language identifiers (PurrceptionLanguageId, LanguageName)
├── index.ts                            — Public barrel export
├── declaration/
│   ├── types.ts                        — Declaration discriminated union types
│   └── helpers/
│       ├── isComponentDeclaration.ts   — Type guard: checks if DeclaredSymbol is a ComponentDeclaration
│       ├── isFunctionDeclaration.ts    — Type guard: checks if DeclaredSymbol is a FunctionDeclaration
│       ├── isInterfaceDeclaration.ts   — Type guard: checks if DeclaredSymbol is an InterfaceDeclaration
│       └── isTypeDeclaration.ts        — Type guard: checks if DeclaredSymbol is a TypeDeclaration
├── jsdoc/
│   ├── types.ts                        — JsDocData, FunctionJsDocData, JsDocTags types
│   └── helpers/
│       └── getFirstTagValue.ts         — Extract first value from a multi-valued JSDoc tag
├── node/
│   ├── types.ts                        — TypeExpressionNode discriminated union (18 kinds)
│   ├── constants.ts                    — BUILTIN_TYPES and BUILTIN_GLOBALS sets
│   └── helpers/
│       ├── createPrimitiveNode.ts      — Factory for PrimitiveNode
│       ├── createTypeRefNode.ts        — Factory for TypeRefNode
│       ├── isBuiltInToken.ts           — Checks if a string is a built-in type name
│       ├── isFunctionTypeNode.ts       — Type guard for FunctionTypeNode
│       ├── isIntersectionTypeNode.ts   — Type guard for IntersectionTypeNode
│       ├── isLiteralTypeNode.ts        — Type guard for LiteralTypeNode (optional value type filter)
│       ├── isMappedTypeNode.ts         — Type guard for MappedTypeNode
│       ├── isObjectLiteralTypeNode.ts  — Type guard for ObjectLiteralTypeNode
│       ├── isTypeExpressionNode.ts     — Type guard for any TypeExpressionNode
│       ├── isTypeRefNode.ts            — Type guard for TypeRefNode
│       └── isUnionTypeNode.ts          — Type guard for UnionTypeNode
└── resolve/
    ├── types.ts                        — ResolveTypeContext type definition
    ├── createResolveTypeContext.ts     — Factory for ResolveTypeContext
    ├── resolveTypeDeclaration.ts       — Top-level entry: resolves a TypeDeclaration
    ├── declarations/
    │   ├── resolveInterfaceDeclaration.ts       — Flattens heritage chain, merges object members
    │   └── resolveTypeExpressionDeclaration.ts  — Wraps resolveExpression for type alias decls
    └── expressions/
        ├── resolveExpression.ts                 — Dispatcher: switch on kind, delegates to per-kind resolvers
        ├── kinds/
        │   ├── resolveTypeRefNode.ts            — Follows type references, resolves via resolveTypeDeclaration
        │   ├── resolveObject.ts                 — Resolves each object member type recursively
        │   ├── resolveIntersection.ts           — Resolves entries, flattens to merged ObjectLiteralTypeNode
        │   ├── resolveUnion.ts                  — Resolves entries, flattens nested unions
        │   ├── resolvePick.ts                   — Resolves Pick<T, K> by filtering object members
        │   ├── resolveOmit.ts                   — Resolves Omit<T, K> by excluding object members
        │   ├── resolveOperator.ts               — Delegates to operator-specific resolvers
        │   └── resolveNodeMember.ts             — Resolves T["member"] access on object types
        ├── normalize/
        │   ├── normalizeObjectLiteral.ts        — Coerces mapped types to object literals
        │   └── normalizeUnion.ts                — Wraps single literals in union nodes
        └── operators/
            └── resolveOperatorKeyOf.ts          — Resolves keyof T by extracting object member names
```
