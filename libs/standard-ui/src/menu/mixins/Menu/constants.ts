import { definePropKeys } from '@no-comply/solid-primitives';

import type { MenuMixinProps } from './types';

export const $MENU_MIXIN = 'mixin:standard:menu';

export const MENU_MIXIN_PROPS = definePropKeys<MenuMixinProps>()([]);
