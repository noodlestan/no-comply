/**
 * Specialized {@link component:IconButton} with customisable icons and label pairs that allows controlling an target identified by `id`.
 */

// @index(['./*.{ts,tsx}', './!(private|parts|functions)*/index.{ts,tsx}'], f => `export * from '${f.path.replace(/\/index$/, '')}';`)
export * from './constants';
export * from './createExpandButton';
export * from './ExpandButton';
export * from './types';
