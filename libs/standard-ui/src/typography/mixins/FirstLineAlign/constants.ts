import { definePropKeys } from '@noodlestan/context-ui-primitives';

import type { FirstLineAlignMixinProps } from './types';

export const FIRST_LINE_ALIGN_MIXIN_PROPS = definePropKeys<FirstLineAlignMixinProps>()(['height']);
