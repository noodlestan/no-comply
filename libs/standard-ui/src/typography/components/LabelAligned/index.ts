/**
 * Renders a {@link component:Label} inside a {@link component:AlignFirstLine} wrapper
 * so that when text wraps to multiple lines, the adjacent elements remain
 * aligned with the first line of the label text.
 */

// @index(['./*.{ts,tsx}', './!(private|parts|functions)*/index.{ts,tsx}'], f => `export * from '${f.path.replace(/\/index$/, '')}';`)
export * from './constants';
export * from './createLabelAligned';
export * from './LabelAligned';
export * from './types';
