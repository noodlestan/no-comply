import { definePropKeys } from '@noodlestan/context-ui-primitives';

import type { SizedActionMixinProps } from './types';

export const SIZED_ACTION_MIXIN_PROPS = definePropKeys<SizedActionMixinProps>()(['size']);
