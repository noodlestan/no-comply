import type { ClassList } from '@no-comply/solid-primitives';

export type AlignToFirstLineMixinProps = {
    aligned?: boolean;
};

export type AlignToFirstLineMixinAPI = {
    $root: {
        classList: ClassList;
    };
};
