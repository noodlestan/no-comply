---
import type { Props } from '@astrojs/starlight/props';
const themeClass = `NUI NUI-ColorScheme-dark NUI-Theme-base NUI-Surface NUI-Surface-stage`;
---

<div class={themeClass}>
    <slot />
</div>
