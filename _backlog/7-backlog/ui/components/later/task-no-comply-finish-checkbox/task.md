# Finish: Checkbox

**Summary:** Checkbox exists but is untested. Verify value binding, indeterminate state, keyboard interaction (Space to toggle), ARIA (`role="checkbox"`, `aria-checked`), and disabled state. Fix any issues found. Likely close to complete.

**Acceptance criteria:**
- Click toggles checked state
- Space key toggles checked state
- `indeterminate` prop works visually
- `disabled` prevents interaction
- ARIA attributes present and correct
