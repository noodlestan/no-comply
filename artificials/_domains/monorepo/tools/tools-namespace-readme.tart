# {{%namespace.name}} — {{%namespace.tagline}}

> {{%namespace.description}}

## Packages

::TEMPLATE for each `%package` in `%namespace.packages`

- `{{%package.name}}` — {{%package.description}} (`{{%package.path}}`).

::END

## {{%project.license.short}}
